import { IonItemDirective } from './ion-item.directive';

describe('IonItemDirective', () => {
  it('should create an instance', () => {
    const directive = new IonItemDirective();
    expect(directive).toBeTruthy();
  });
});
